<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-xs-9 col-md-8">
    <% @projects.each do |project| %>
      <div class="panel panel-info">
        <div class="panel-heading">
          <h3 class="panel-title"><%= project.title %></h3>
        </div>
        <div class="panel-body">
          <strong>Technologies Used:</strong><br />
          <%= project.technologies %> <br /><br />
          <strong>Description:</strong> <br />
          <p><%= project.description.html_safe %></p><br />
        </div>
        <div class="panel-footer">
          <%= link_to 'Show', project if policy(project).show? %>
          <%= link_to 'Edit', edit_project_path(project) if policy(project).edit? %>
          <%= link_to 'Destroy', project, method: :delete, data: { confirm: "Are you sure?" } if policy(project).destroy? %>
        </div>
      </div>
    <% end %>
  </div>
  <div class="col-xs-3 col-md-4">
    <% if policy(Project).new?  %>
      <%= link_to 'New Project', new_project_path %>
    <% end %>
    <p><b>My Projects:</b><br/>
    A collection of major projects that I have completed, worked on,
    or am in the process of developing.  A brief description of the technologies used
    as well as a general purpose description of my role, participation, and lessons
    learned.  For projects I am tinkering with please visit my <a href="http://github.com/trendwithin">github.</a></P>
  </div>
</div>



